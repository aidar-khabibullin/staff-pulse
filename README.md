# Тестовое задание

`frontend` · `react` · `typescript`

## Задача

Реализовать дашборд для мониторинга орг-структуры компании.
Структура иерархическая: дивизионы → отделы → команды.
У каждого узла — численность сотрудников, бюджет и метрика эффективности.
Нужен интерактивный просмотр дерева и аналитическая таблица с агрегированными показателями.

## Стек

React, Vite, TypeScript.

**Будет плюсом:** использование styled-components для стилизации.

## Нефункциональные требования

- **Надёжность:** обработка состояний загрузки, ошибки и пустого ответа; корректная отмена запросов при размонтировании компонента.
- **Эффективность обращения к API:** кэширование ответов, избегание лишних запросов (stale-while-revalidate или аналог); инвалидация кэша только при реальном изменении данных.

> **Обязательно:** AI-инструменты (Claude, Cursor, Copilot и др.) в процессе разработки. В README опишите: что генерировали, что переписали руками и почему. См. раздел [AI в разработке](#ai-в-разработке).

## Mock API

Реализуйте собственный сервер. `GET /api/org-tree` возвращает плоский массив:

```
{ id, name, parentId, headcount, budget, performance (0–100), updatedAt }
```

Минимум 40 узлов, не менее трёх уровней вложенности.

## Этапы

Каждый этап — отдельный коммит с тегом `step/N`.

### 01 FOUNDATION — Проект, API, дерево

- Scaffold: Vite + React + TypeScript, абсолютные импорты
- Схема API-ответа валидируется на клиенте; невалидный ответ — ошибка
- Запрос через слой кэширования, stale time 5 секунд
- Интерактивное дерево: раскрытие/скрытие ветвей, второй уровень открыт по умолчанию
- Каждый узел показывает `name`, `headcount` и цветовой индикатор `performance`
- Состояния: загрузка, ошибка, пустой ответ; никакого inline-CSS

### 02 CORE — Аналитическая таблица

- Переключатель «Дерево / Таблица» (или split-view ≥1280px)
- Столбцы: Подразделение, Уровень, Всего сотрудников, Бюджет суммарный, Средняя эффективность
- Суммарные показатели включают данные узла и всех его потомков; средняя эффективность — взвешенная по headcount
- Сортировка по любому столбцу, двойной клик — обратная
- Фильтр по названию: real-time, дебаунс 250мс
- Клик по строке выделяет узел в дереве; бюджеты в формате `12 345 678 руб.`
- Агрегация считается один раз после загрузки данных и мемоизируется

### 03 POLISH — Live-обновления и UX

- Сервер поддерживает обновления в реальном времени. Клиент реализует один из вариантов: WebSocket, SSE или эффективный поллинг (с учётом частоты изменений, backoff и минимизации лишних запросов)
- Клиент применяет патч без полного рефетча; обновлённые ячейки — fade-out ~1.5с
- Пересчёт агрегатов только для затронутого узла и его предков
- Индикатор соединения в шапке; экспоненциальный backoff при обрыве
- Keyboard navigation по таблице: стрелки, Home/End, Enter
- Анимация раскрытия дерева через height transition; `prefers-reduced-motion` уважается

### 04 BONUS — Production + AI-фича

- Docker: `docker-compose up` поднимает клиент и сервер; конфиг через `.env`
- Nginx: проксирует API, отдаёт статику с gzip; production-сборка ≤200 КБ gzip
- AI-поиск: строка поиска принимает естественный язык; ответ — структурированный фильтр на клиенте; fallback — текстовый поиск

## Что сдавать

- Публичный репозиторий или архив с исходным кодом; каждый этап — отдельный коммит с тегом `step/N`
- README: запуск одной командой + раздел «AI в разработке» (обязательно)
- Unit-тест на функцию агрегации; скриншоты или GIF финального результата
- `docs/architecture.md` — слои приложения, поток данных от API до UI
- `docs/data-model.md` — дерево, алгоритм агрегации, контракт WebSocket-патча
- `docs/adr/NNN-название.md` — ADR для нетривиальных решений (контекст, решение, альтернативы, последствия)

> Не нужно: авторизацию, БД, UI-библиотеки (MUI, Ant и т.п.). Всё, что не оговорено явно — на ваше усмотрение.

## Документация проекта

- [`docs/prd-org-structure-dashboard.md`](docs/prd-org-structure-dashboard.md) — PRD
- [`docs/plan-org-structure-dashboard.md`](docs/plan-org-structure-dashboard.md) — план фаз реализации, отмечается по ходу работы
- [`CLAUDE.md`](CLAUDE.md) — рабочие правила проекта для Claude Code

## Запуск проекта

> Инструкция запуска одной командой (Docker) появится в Фазе 7 (BONUS). Ниже — текущий способ запуска по частям, актуальный после Фазы 2.

Mock API сервер:

```bash
cd server
npm install
npm run dev
```

Сервер поднимется на `http://localhost:4000`, эндпоинт — `GET /api/org-tree`.

Frontend-клиент:

```bash
cd client
npm install
npm run dev
```

Клиент поднимется на `http://localhost:5173` и обращается к API сервера на порту 4000.

E2E-тесты (Playwright, из корня репозитория):

```bash
npm install
npx playwright install chromium
npm run test:e2e
```

## AI в разработке

Проект разрабатывается в паре с **Claude Code** (модель Claude Sonnet 5) — весь код, конфигурация и документация на данный момент сгенерированы ассистентом по шагам, под контролем и с подтверждением пользователя на каждом этапе (структура веток, состав фаз, инструменты тестирования и т.д. — решения пользователя, реализация — ассистента). Ручных правок кода на данный момент не вносилось; если они появятся, здесь будет зафиксировано, что именно и почему было переписано руками.

Фаза 2 (frontend scaffold): использованы `npm create vite@latest` для scaffold-а и стандартный набор для валидации/тестирования (`zod`, `vitest`, `@testing-library/react`) — их подключение и сама реализация слоя данных полностью сгенерированы ассистентом.

По мере реализации фаз раздел будет дополняться.

### Что сгенерировано

- **Инициализация репозитория:** `git init`, `.gitignore`, структура веток (`main` + `phase/N-<slug>` фича-ветки, мердж через `--no-ff`)
- **Фаза 1 (mock API сервер):** Express + TypeScript сервер, генератор мок-данных орг-структуры (`server/src/data/generateOrgTree.ts`), проверка целостности `parentId`, конфигурация порта/CORS, unit-тесты на генератор (Vitest)
- **E2E-инфраструктура:** конфигурация Playwright (`playwright.config.ts`, project `api`), тест контракта `GET /api/org-tree` (`e2e/org-tree.api.spec.ts`)
- **`CLAUDE.md`** — рабочие правила проекта для дальнейшей разработки с Claude Code
- **Фаза 2 (frontend scaffold и слой данных):** scaffold Vite + React + TypeScript (`client/`), абсолютные импорты (`@/*`), zod-схема и клиент `GET /api/org-tree` с валидацией (`client/src/shared/api/`), кэширующий хук `useOrgTree` (stale-while-revalidate, stale time 5с, отмена запроса через `AbortController` при unmount) (`client/src/shared/lib/useOrgTree.ts`), состояния загрузки/ошибки/пустого ответа без inline-CSS (CSS Modules), unit-тесты на отмену запроса (Vitest + Testing Library), project `ui` в `playwright.config.ts` и Playwright-тест загрузки данных (`e2e/org-tree.ui.spec.ts`)

### Что переписано руками и почему

Пока ничего — весь код по-прежнему сгенерирован ассистентом. Раздел будет обновляться, если ассистентская реализация потребует ручной корректировки.
